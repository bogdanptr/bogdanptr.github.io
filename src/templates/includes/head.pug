//- Page specific vars
block vars

//- block prepend head || block append head
block head
  meta(charset = "utf-8")
  title #{page.title}
  meta(name = "description", content = page.description)
  meta(name = "viewport", content = "width=device-width, initial-scale=1")
  link(rel = "stylesheet", href = "/assets/css/style.css")
